package com.sora.aitravel.common.enums;

/**
 * 预算口径枚举。
 *
 * <p>标识行程预算的计算方式：整趟总预算（TOTAL）或人均预算（PER_PERSON）。
 */
public enum BudgetTypeEnum {
    /** 整趟总预算。 */
    TOTAL,
    /** 人均预算。 */
    PER_PERSON
}
