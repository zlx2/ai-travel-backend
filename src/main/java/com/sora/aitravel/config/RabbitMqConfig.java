package com.sora.aitravel.config;

import org.springframework.context.annotation.Configuration;

/**
 * RabbitMQ 消息队列配置。
 *
 * <p>一期仅保留 Spring Boot 的自动配置（Auto-Configuration）， 暂不接入主业务流程中的消息队列功能。
 */
@Configuration
public class RabbitMqConfig {
    // 一期仅保留自动配置，不接入主业务流程。
}
